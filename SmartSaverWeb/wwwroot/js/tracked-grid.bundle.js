// TEMP: Direct inline version so page loads immediately
const rootElement = document.getElementById("tracked-grid-root");
rootElement.innerHTML = `
    <div style="padding:20px; font-size:1.2rem;">
        🚀 React base page is working — waiting for Replit import
    </div>
`;
